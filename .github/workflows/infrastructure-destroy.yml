name: Infrastructure Destroy

on:
  workflow_dispatch:

jobs:
  destroy:
    runs-on: ubuntu-latest

    steps:
      - name: Placeholder
        run: echo "Infrastructure destroy workflow will be implemented next."